{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Home Assistant (.dev_config)",
      "type": "debugpy",
      "request": "launch",
      "module": "homeassistant",
      "preLaunchTask": "Link pact_coffee integration",
      "args": [
        "--config",
        "${workspaceFolder}/.dev_config"
      ],
      "cwd": "${workspaceFolder}",
      "console": "integratedTerminal"
    }
  ]
}
